import 'package:flutter/material.dart';

class TankPlannerScreen extends StatelessWidget {
  const TankPlannerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tank Planner - Coming Soon!'),
    );
  }
}
